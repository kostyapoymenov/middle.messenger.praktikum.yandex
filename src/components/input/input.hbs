<input 
  type="{{type}}" 
  name="{{name}}" 
  id="{{id}}" 
  value="{{value}}" 
  placeholder="{{placeholder}}" 
  class=" input {{class}} input--{{variant}}" 
  {{#if disabled}}disabled{{/if}} 
  {{#if required}}required{{/if}} 
/>
